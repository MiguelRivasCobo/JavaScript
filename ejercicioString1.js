function invertir(cad_arg) {
var x = promtp("Introduzca una cadena")
   x = cad_Arg.length;
  var cadenaInvertida = "";
 
  while (x>=0) {
    cadenaInvertida = cadenaInvertida + cadena.charAt(x);
    x--;
  }
  return cadenaInvertida;
  document.write(+cadenaInvertida);
}